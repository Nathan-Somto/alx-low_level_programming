# include "main.h"
# include <stdio.h>
/**
 * mul - calcualtes the multiplication of numbers
 * @a: first parameter
 * @b: second parameter
 * Return: returns the result
*/
int mul(int a, int b)
{
	return (a * b);
}
